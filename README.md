# cmtx-api-public
Servicio **API pública** (Python) que consume DATA y retorna el texto.

## Qué incluye
- Dockerfile + app mínima en Python
- Deployment + Service (Helm)
- Vars de entorno para apuntar a DATA
- CI/CD: build imagen + deploy Helm
